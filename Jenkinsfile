pipeline {
  agent any
  stages {
    stage('test01') {
      steps {
        stash(name: '缓存文件的标记我也不知道该写什么', allowEmpty: false, excludes: null, includes: null, useDefaultExcludes: false)
      }
    }

  }
}